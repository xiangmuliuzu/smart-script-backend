<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartscript.platform.user.mapper.AppUserMapper">

    <resultMap id="AppUserResult" type="com.smartscript.platform.user.domain.AppUserRecord">
        <id property="userId" column="user_id"/>
        <result property="userName" column="user_name"/>
        <result property="nickName" column="nick_name"/>
        <result property="phonenumber" column="phonenumber"/>
        <result property="password" column="password"/>
        <result property="userType" column="user_type"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="appUserColumns">
        user_id, user_name, nick_name, phonenumber, password, user_type, avatar, status, del_flag
    </sql>

    <select id="selectByPhone" resultMap="AppUserResult">
        SELECT <include refid="appUserColumns"/>
        FROM sys_user
        WHERE phonenumber = #{phone}
        LIMIT 1
    </select>

    <select id="selectById" resultMap="AppUserResult">
        SELECT <include refid="appUserColumns"/>
        FROM sys_user
        WHERE user_id = #{userId}
        LIMIT 1
    </select>

    <insert id="insertAppUser" parameterType="com.smartscript.platform.user.domain.AppUserRecord"
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO sys_user
        (user_name, nick_name, phonenumber, password, user_type, avatar, status, del_flag, email, sex,
         create_by, create_time, remark)
        VALUES
        (#{userName}, #{nickName}, #{phonenumber}, #{password}, #{userType}, #{avatar}, #{status}, #{delFlag}, '',
         '0', 'app-auth', NOW(), '')
    </insert>

    <update id="updatePassword">
        UPDATE sys_user
        SET password = #{password}, pwd_update_date = NOW(), update_time = NOW(), update_by = 'app-auth'
        WHERE user_id = #{userId}
    </update>

    <update id="updateLoginInfo">
        UPDATE sys_user
        SET login_ip = #{loginIp}, login_date = #{loginDate}, update_time = #{loginDate}
        WHERE user_id = #{userId}
    </update>

    <select id="selectRealNameStatus" resultType="string">
        SELECT status
        FROM user_real_name_auth
        WHERE user_id = #{userId}
        ORDER BY id DESC
        LIMIT 1
    </select>
</mapper>
