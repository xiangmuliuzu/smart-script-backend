<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartscript.platform.trade.mapper.SysPartnerMapper">

    <resultMap type="com.smartscript.platform.trade.domain.SysPartner" id="SysPartnerResult">
        <result property="partnerId"       column="partner_id" />
        <result property="partnerNo"       column="partner_no" />
        <result property="partnerName"     column="partner_name" />
        <result property="partnerType"     column="partner_type" />
        <result property="contactPerson"   column="contact_person" />
        <result property="contactPhone"    column="contact_phone" />
        <result property="contactEmail"    column="contact_email" />
        <result property="address"         column="address" />
        <result property="demandTags"      column="demand_tags" />
        <result property="cooperationCount" column="cooperation_count" />
        <result property="totalAmount"     column="total_amount" />
        <result property="status"          column="status" />
        <result property="remark"          column="remark" />
        <result property="createBy"        column="create_by" />
        <result property="createTime"      column="create_time" />
        <result property="updateBy"        column="update_by" />
        <result property="updateTime"      column="update_time" />
        <result property="createdAt"       column="created_at" />
        <result property="updatedAt"       column="updated_at" />
    </resultMap>

    <sql id="selectPartnerVo">
        select partner_id, partner_no, partner_name, partner_type, contact_person, contact_phone,
               contact_email, address, demand_tags, cooperation_count, total_amount, status,
               remark, create_by, create_time, update_by, update_time, created_at, updated_at
        from sys_partner
    </sql>

    <select id="selectPartnerList" parameterType="com.smartscript.platform.trade.domain.SysPartner" resultMap="SysPartnerResult">
        <include refid="selectPartnerVo"/>
        <where>
            <if test="partnerName != null and partnerName != ''">
                AND partner_name like concat('%', #{partnerName}, '%')
            </if>
            <if test="partnerType != null and partnerType != ''">
                AND partner_type = #{partnerType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by created_at desc
    </select>

    <select id="selectPartnerById" parameterType="Long" resultMap="SysPartnerResult">
        <include refid="selectPartnerVo"/>
        where partner_id = #{partnerId}
    </select>

    <insert id="insertPartner" parameterType="com.smartscript.platform.trade.domain.SysPartner" useGeneratedKeys="true" keyProperty="partnerId">
        insert into sys_partner (
            partner_no, partner_name, partner_type, contact_person, contact_phone,
            contact_email, address, demand_tags, cooperation_count, total_amount,
            status, remark, create_by, create_time
        ) values (
            #{partnerNo}, #{partnerName}, #{partnerType}, #{contactPerson}, #{contactPhone},
            #{contactEmail}, #{address}, #{demandTags}, #{cooperationCount}, #{totalAmount},
            #{status}, #{remark}, #{createBy}, #{createTime}
        )
    </insert>

    <update id="updatePartner" parameterType="com.smartscript.platform.trade.domain.SysPartner">
        update sys_partner
        <set>
            <if test="partnerName != null">partner_name = #{partnerName},</if>
            <if test="partnerType != null">partner_type = #{partnerType},</if>
            <if test="contactPerson != null">contact_person = #{contactPerson},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="contactEmail != null">contact_email = #{contactEmail},</if>
            <if test="address != null">address = #{address},</if>
            <if test="demandTags != null">demand_tags = #{demandTags},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where partner_id = #{partnerId}
    </update>

    <delete id="deletePartnerByIds" parameterType="String">
        delete from sys_partner where partner_id in
        <foreach item="partnerId" collection="array" open="(" separator="," close=")">
            #{partnerId}
        </foreach>
    </delete>

</mapper>
